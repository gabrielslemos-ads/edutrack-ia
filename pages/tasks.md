# Tarefas: API de Busca para Disciplinas

- [ ] **XanoScript / Python:** Implementar a lógica do endpoint `GET /subjects/search` que consulta as tabelas `subjects` e `academic_tasks` para filtrar por nome e/ou tarefas atrasadas, garantindo que o usuário (`auth.id`) só acesse seus próprios dados.